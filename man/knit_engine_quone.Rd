% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr.R
\name{knit_engine_quone}
\alias{knit_engine_quone}
\title{knitr / Quarto engine for \verb{```\{quone\}} chunks}
\usage{
knit_engine_quone(options)
}
\arguments{
\item{options}{A \code{knitr} chunk options list.}
}
\value{
The chunk output, suitable for splicing into the rendered
document.
}
\description{
Compiles each Quone chunk via \code{\link[=build]{build()}}, optionally \code{source()}s the
generated R, and renders one of three views:
}
\details{
\itemize{
\item \code{quone.show = "quone"} -- show only the Quone source.
\item \code{quone.show = "r"} -- show only the generated R.
\item \code{quone.show = "both"} (the default) -- side-by-side.
}

Registered automatically in \code{.onLoad} whenever \code{knitr} is loaded.
Callers who want to register manually can do
\code{knitr::knit_engines$set(quone = quone:::knit_engine_quone)}.
}
\keyword{internal}
