% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repl.R
\name{repl}
\alias{repl}
\title{Start an interactive Quone REPL}
\usage{
repl(project = detect_project(), rscript_path = NULL)
}
\arguments{
\item{project}{Project root, or \code{NULL} to start without a
project context.}

\item{rscript_path}{Optional path to a specific \code{Rscript} to use as
the REPL backend; defaults to whichever \code{Rscript} is on PATH.}
}
\value{
Invisibly, the REPL's exit status.
}
\description{
Spawns \verb{quonec repl} and gives it the calling terminal. When
\code{project} is non-\code{NULL} (the default when a \code{quone.toml} is
present in the current directory), the REPL auto-loads every
\code{.Q} file under \verb{src/} on startup.
}
\details{
Returns when the user exits the REPL with \verb{:quit} (or end-of-input).
}
