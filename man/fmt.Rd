% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt.R
\name{fmt}
\alias{fmt}
\title{Format a Quone file with \verb{quonec fmt}}
\usage{
fmt(path, check = FALSE)
}
\arguments{
\item{path}{Path to a \code{.Q} file (or a project root, in which case
every \verb{src/**/*.Q} is formatted recursively).}

\item{check}{Run in check mode (no rewrite).}
}
\value{
Invisibly, \code{TRUE} on success / no changes needed.
}
\description{
Wraps the elm-format-style canonical formatter built into the
compiler (compiler track A4). When \code{check = TRUE}, the file is
read but not modified; the function returns \code{TRUE} if the input
is already canonical, \code{FALSE} otherwise (and prints a unified
diff to stderr).
}
