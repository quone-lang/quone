% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{setup}
\alias{setup}
\title{One-step setup for the Quone toolchain}
\usage{
setup(prompt = interactive(), editor = "auto")
}
\arguments{
\item{prompt}{If \code{TRUE} (the default in interactive sessions), ask
for confirmation before any install step. If \code{FALSE}, proceed
without asking. In non-interactive sessions this is forced to
\code{FALSE}.}

\item{editor}{Which editor(s) to configure. Passed through to
\code{\link[=install_lsp]{install_lsp()}}; defaults to \code{"auto"} which configures every
editor it detects.}
}
\value{
Invisibly, a list with \code{compiler} (path to \code{quonec}) and
\code{lsp} (the tibble \code{\link[=install_lsp]{install_lsp()}} returned, or \code{NULL} if the
user declined).
}
\description{
\code{setup()} is the recommended way to get going with \code{{quone}} from a
fresh R install. It walks you through:
}
\details{
\enumerate{
\item installing the \href{https://github.com/quone-lang/compiler}{\code{quonec}}
compiler if it isn't already on your machine, and persisting
its location to \verb{~/.Renviron} so future R sessions find it
automatically;
\item wiring up Quone's language server in any LSP-aware editor it
can detect (VS Code, Cursor, Positron, RStudio, Neovim, Helix,
Zed);
\item printing a short "what's next?" summary.
}

By default \code{setup()} is interactive and asks you to confirm each
step that touches your machine. In non-interactive sessions
(\code{Rscript}, CI, etc.) it auto-confirms so it can run unattended;
you can force the same behaviour from an interactive session with
\code{setup(prompt = FALSE)}.
}
