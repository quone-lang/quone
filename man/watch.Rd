% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watch.R
\name{watch}
\alias{watch}
\title{Watch a Quone project and rebuild on every change}
\usage{
watch(
  project = ".",
  interval_ms = 500,
  on_success = NULL,
  on_error = NULL,
  block = !interactive()
)
}
\arguments{
\item{project}{Project root.}

\item{interval_ms}{Polling interval in milliseconds.}

\item{on_success}{Optional callback invoked after a successful
build, receiving the build directory.}

\item{on_error}{Optional callback invoked on a build failure,
receiving the diagnostics tibble. Defaults to
\code{\link[=show_diagnostics]{show_diagnostics()}}.}

\item{block}{If \code{TRUE}, blocks the caller and pumps the event
loop in-line (equivalent to \code{while (TRUE) later::run_now(0.1)}); if \code{FALSE} (the default in interactive
sessions), schedules the poll on R's main event loop and
returns immediately.}
}
\value{
Invisibly, an opaque watcher handle (currently a
one-element environment) that you can pass to \code{\link[=watch_stop]{watch_stop()}}.
}
\description{
Polls \verb{src/} (every \code{interval_ms} milliseconds; default 500 ms) on
R's \code{\link[later:later]{later::later()}} event loop. The polling task re-schedules
itself, so the user's REPL stays responsive: type, run other
commands, or call \code{\link[=watch_stop]{watch_stop()}} to terminate the watcher.
}
\details{
Use \code{\link[=watch_stop]{watch_stop()}} (or \code{Ctrl-C} in the rare case the loop is
actively executing a build) to stop.
}
