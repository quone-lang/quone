% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lsp.R
\name{build_vsix}
\alias{build_vsix}
\title{Build the bundled VS Code / Cursor / Positron extension}
\usage{
build_vsix(src = NULL, quiet = FALSE)
}
\arguments{
\item{src}{Optional path to the extension source. Defaults to the
sibling \verb{../compiler/editors/vscode/} checkout. An error is
raised if no source can be found.}

\item{quiet}{If \code{TRUE}, hide \code{npm} output (errors are still
printed). Defaults to \code{FALSE}.}
}
\value{
The absolute path to the freshly built \code{.vsix} (invisibly).
}
\description{
Runs \verb{npm install} and \verb{npm run package} inside the
\verb{compiler/editors/vscode/} source tree to produce a \code{.vsix} that
\code{\link[=install_lsp]{install_lsp()}} and \code{\link[=setup]{setup()}} can install into VS Code, Cursor, or
Positron. Most users won't need to call this directly --
\code{install_lsp()} calls it for you when no prebuilt extension is
shipped.
}
