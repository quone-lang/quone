% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watch.R
\name{dev_session}
\alias{dev_session}
\title{Build, then \code{pkgload::load_all} the generated R package}
\usage{
dev_session(project = ".", interval_ms = 500, block = !interactive())
}
\arguments{
\item{project}{Project root.}

\item{interval_ms}{Polling interval in milliseconds.}

\item{block}{If \code{TRUE}, blocks the caller and pumps the event
loop in-line; if \code{FALSE} (the default in interactive sessions),
schedules the watcher on R's main event loop and returns
immediately so the user can keep typing.}
}
\value{
Invisibly, the watcher handle.
}
\description{
Like \code{devtools::load_all()} but for a Quone project: every change
triggers a rebuild plus a fresh \code{load_all} so the freshly-defined
R functions are reachable in the calling session immediately.
}
\details{
Implemented on top of \code{\link[=watch]{watch()}} so the polling runs on R's event
loop; the calling session's REPL stays interactive.
}
